org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false

# --- Kotlin compiler workaround (fixes "Storage is already registered" /
# "Could not close incremental caches" daemon crashes on Windows) ---
# Run the Kotlin compiler inside the Gradle process instead of a separate
# daemon, and disable incremental compilation so the shared build dir
# (see build.gradle.kts) can't produce colliding cache registrations.
kotlin.compiler.execution.strategy=in-process
kotlin.incremental=false
