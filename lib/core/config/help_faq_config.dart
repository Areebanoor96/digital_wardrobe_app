/// Configuration for the Digital Wardrobe help/FAQ destination.
///
/// The product will publish a web help/FAQ page. Until that URL is finalized
/// it is intentionally empty — do not guess or hardcode a placeholder domain.
/// When a real URL is confirmed, set [webUrl] here and wire the open action in
/// `HelpFaqScreen`.
class HelpFaqConfig {
  const HelpFaqConfig._();

  static const String webUrl = '';
}